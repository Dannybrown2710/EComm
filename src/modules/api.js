/*import { NetworkApi } from '../NetworkApi';
export const ADD_LINKS = "api/ADD_LINKS";
const initialState = {}
export default (state = initialState, action) => {
  console.log(state);
  switch (action.type) {
    case ADD_LINKS:
      return {
        ...state,
        ...NetworkApi
      }
    default:
      return state
  }
}*/
